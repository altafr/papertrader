{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "pnpm --filter @momentum/worker build",
    "watchPatterns": [
      "/apps/worker/**",
      "/packages/alpaca/**",
      "/packages/config/**",
      "/packages/db/**",
      "/packages/domain/**",
      "/package.json",
      "/pnpm-lock.yaml",
      "/pnpm-workspace.yaml",
      "/tsconfig.base.json"
    ]
  },
  "deploy": {
    "healthcheckPath": "/health",
    "healthcheckTimeout": 120,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10,
    "startCommand": "pnpm --filter @momentum/worker start"
  }
}
